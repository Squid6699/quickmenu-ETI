import { View } from "react-native";
import { Text } from "react-native-paper";

const Order = () => {

    return (
        <View>
            <Text>Order</Text>
        </View>
    );
}

export default Order;